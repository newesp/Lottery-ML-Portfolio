import { Callout, Metric, PageIntro } from "../components/UI";
import { locked, num } from "../lib/results";

function AreaResult({name, data}:{name:string;data:typeof locked.areas.area1}){const ci=data.paired_bootstrap_hits_vs_uniform;return <article className="result-card"><p className="eyebrow">{name}</p><h2>{data.selection.model.replace("_"," ")}</h2><p className="muted">{data.selection.feature_set} · 3-seed ensemble</p><div className="result-metrics"><Metric value={num(data.ensemble_metrics.average_hits)} label="Model hits"/><Metric value={num(data.baselines.uniform.average_hits)} label="Uniform hits"/><Metric value={num(data.baselines.rolling_frequency.average_hits)} label="Rolling hits"/></div><div className="ci"><span>Δ hits vs Uniform</span><strong>{ci.estimate>0?"+":""}{num(ci.estimate)}</strong><div className="ci-line"><i/><b style={{left:`${Math.max(0,Math.min(100,(-ci.lower/(ci.upper-ci.lower))*100))}%`}}/></div><small>95% CI [{num(ci.lower)}, {num(ci.upper)}]</small></div></article>}
export default function EvaluationPage(){return <div className="section-shell page"><PageIntro eyebrow="LOCKED HOLDOUT · 2024+" title="只看一次的最終評估"><p>模型與 feature set 在 development artifact 中選定並以 SHA-256 鎖定。2024+ 共 {locked.holdout_draw_count} 期，不參與 fitting、tuning 或選型。</p></PageIntro><div className="results-grid"><AreaResult name="第一區 · Top 6" data={locked.areas.area1}/><AreaResult name="第二區 · Top 1" data={locked.areas.area2}/></div><Callout tone="warning" title="點估計不是證據"><p>兩區模型對 Uniform 的差值皆為正，但 10,000 次 paired draw bootstrap 的 95% CI 都跨越 0；同時 Rolling Frequency baseline 在兩區都略高。這些結果不足以主張可泛化的預測優勢。</p></Callout><section className="content-section"><h2>為什麼還看 Brier score 與 Log loss？</h2><p className="body-copy">Top-k hits 只看排序後是否命中；Brier score 與 log loss 會懲罰失準的機率。模型即使偶然多中幾個號碼，也不代表它的 probability estimates 可信。多指標能降低只挑好看結果的風險。</p></section></div>}
